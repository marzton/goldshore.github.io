{
  "github": {
    "token": "${GH_TOKEN}"
  },
  "cloudflare": {
    "apiToken": "${CF_API_TOKEN}",
    "accountId": "${CF_ACCOUNT_ID}",
    "zoneId": "${CF_ZONE_ID}"
  },
  "agent": {
    "webhookUrl": "${AGENT_WEBHOOK_URL}"
  }
}
