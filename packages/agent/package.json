{
  "name": "@goldshore/agent",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && printf 'Agent build placeholder\n' > dist/README.md",
    "dev": "echo 'agent runtime wiring pending'",
    "lint": "echo 'lint not yet configured for @goldshore/agent'",
    "typecheck": "echo 'typecheck not yet configured for @goldshore/agent'"
  }
}
