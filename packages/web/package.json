{
  "name": "@goldshore/web-package",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && printf '<!-- placeholder web build -->\n' > dist/index.html",
    "dev": "echo 'Run marketing site dev server via Pages project'",
    "lint": "echo 'lint not yet configured for @goldshore/web'",
    "typecheck": "echo 'typecheck not yet configured for @goldshore/web'"
  }
}
