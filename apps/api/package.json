{
  "name": "@goldshore/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=neutral --target=es2022 --outdir=dist --sourcemap && npm run -w @goldshore/api-worker build",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.23.0",
    "miniflare": "^3.20240904.0",
    "typescript": "^5.6.2",
    "vitest": "^1.6.0"
  }
}
